import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Turbopack otomatis di dev (Next.js 15+)
  // Edge runtime diterapkan per route via `export const runtime = 'edge'`
}

export default nextConfig
